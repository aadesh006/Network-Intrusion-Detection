# Network Intrusion Detection — CICIDS2017

A machine learning pipeline for detecting and classifying network intrusions
using the [CICIDS2017](https://www.unb.ca/cic/datasets/ids-2017.html) dataset
(Canadian Institute for Cybersecurity, University of New Brunswick).

## Overview

This project trains classifiers on flow-level network traffic features
(extracted via CICFlowMeter) to:

1. **Binary classification** — distinguish `BENIGN` traffic from `ATTACK`
   traffic.
2. **Multiclass classification** — identify the *specific* attack category
   (DoS Hulk, PortScan, DDoS, Brute Force, Web Attacks, Bot, Infiltration,
   Heartbleed, etc.).

The dataset spans 5 days of network activity and ~2.8 million labeled flows,
with a realistic class imbalance (the vast majority of traffic is benign,
while some attack types like Heartbleed have only a handful of examples).
Handling this imbalance — via stratified splitting, `class_weight`/sample
weighting, grouping ultra-rare classes, and using macro-F1 / per-class
precision-recall rather than raw accuracy — is a core part of the
methodology, not an afterthought.

## Project structure

```
network-intrusion-detection/
├── data/
│   ├── raw/              # place the 8 CICIDS2017 CSVs here (not committed)
│   └── processed/        # cleaned/labeled parquet, generated by notebook 01
├── notebooks/
│   ├── 01_eda_and_preprocessing.ipynb
│   └── 02_modeling_and_evaluation.ipynb
├── src/
│   ├── data_loader.py    # loads & concatenates raw CSVs
│   ├── preprocessing.py  # cleaning, label encoding, train/test split + scaling
│   └── evaluation.py     # plotting & metric helpers
├── outputs/
│   ├── figures/          # saved plots (generated by notebooks)
│   └── models/           # saved models, scalers, encoders (generated)
├── app.py                 # Streamlit dashboard for exploring predictions
└── requirements.txt
```

## Setup

```bash
python -m venv venv
source venv/bin/activate   # or venv\Scripts\activate on Windows
pip install -r requirements.txt
```

## Getting the data

1. Go to https://www.unb.ca/cic/datasets/ids-2017.html and download the
   **"MachineLearningCSV"** (also distributed as `GeneratedLabelledFlows`)
   archive — 8 CSV files, one per day/scenario:
   - `Monday-WorkingHours.pcap_ISCX.csv`
   - `Tuesday-WorkingHours.pcap_ISCX.csv`
   - `Wednesday-workingHours.pcap_ISCX.csv`
   - `Thursday-WorkingHours-Morning-WebAttacks.pcap_ISCX.csv`
   - `Thursday-WorkingHours-Afternoon-Infilteration.pcap_ISCX.csv`
   - `Friday-WorkingHours-Morning.pcap_ISCX.csv`
   - `Friday-WorkingHours-Afternoon-PortScan.pcap_ISCX.csv`
   - `Friday-WorkingHours-Afternoon-DDos.pcap_ISCX.csv`
2. Extract all 8 CSVs into `data/raw/`.

## Running the pipeline

1. **`notebooks/01_eda_and_preprocessing.ipynb`**
   Loads the raw CSVs, explores class distributions and data quality
   issues (infinite values, duplicates, zero-variance columns), cleans the
   data, builds binary and multiclass label columns, and saves a processed
   parquet to `data/processed/`.

2. **`notebooks/02_modeling_and_evaluation.ipynb`**
   Trains and compares Logistic Regression, Random Forest, and XGBoost on
   both the binary and multiclass tasks. Evaluates with confusion matrices,
   per-class precision/recall/F1, macro-F1, and ROC-AUC. Saves the best
   models to `outputs/models/`.

3. **`streamlit run app.py`**
   Interactive dashboard: explore the dataset's class balance, run the
   trained models on random sample flows, and manually tweak feature values
   to see how predictions change.

## Methodology notes

- **Why not just accuracy?** ~80% of CICIDS2017 traffic is benign. A model
  that predicts "BENIGN" for everything scores ~80% accuracy while being
  useless. We report per-class precision/recall/F1, macro-F1 (treats every
  class equally regardless of size), and ROC-AUC.
- **Why group rare classes?** A handful of attack types (Heartbleed,
  Infiltration) have so few samples that a stratified train/test split can't
  guarantee both splits get examples. Classes below a configurable threshold
  (`MIN_CLASS_COUNT` in `src/preprocessing.py`) are grouped into `"Other"`
  for the multiclass task.
- **Why Random Forest / XGBoost over deep learning?** This is tabular,
  flow-level data with ~70 engineered features and clear non-linear
  decision boundaries (e.g. PortScan flows cluster around near-zero
  payload sizes). Tree ensembles are a strong, fast, and interpretable fit
  — feature importance directly tells you which traffic characteristics
  signal which attacks, which matters for a security application.

## Caveats

CICIDS2017 traffic was generated in a controlled university testbed over a
fixed time window. Absolute performance numbers (which are typically very
high, >99% on several attack types) won't transfer directly to a production
network with different traffic patterns and attack tooling — but the
*methodology* (handling imbalance, evaluation choices, feature importance
analysis) generalizes to real-world intrusion detection systems.

## Credits

Dataset: Sharafaldin, I., Lashkari, A.H., and Ghorbani, A.A., "Toward
Generating a New Intrusion Detection Dataset and Intrusion Traffic
Characterization", ICISSP 2018.
